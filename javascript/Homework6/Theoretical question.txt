1.Explain in your own words how the forEach loop works.
The forEach loop is used inside the function in order to call a function once for each element inside the array.
The function cannot work without the values for array elements.